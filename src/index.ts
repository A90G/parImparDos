// par impar avanzado
//let cero: number = 0;
let numero: number = Number(prompt("ingrese un numero entero"));

while (numero === 0) {
  console.log( Number(prompt("ingrese un nùmero mayor a cero");
} 
  if (numero % 2 === 0) {
    console.log("el número es par", numero);
  } else {
    console.log("el número es impar", numero);
  }
